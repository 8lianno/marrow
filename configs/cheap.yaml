# Cheap preset: no API calls, local-only or stubbed where possible.
extends: default.yaml

mode: host

models:
  synthesis:
    provider: vllm
    model_id: meta-llama/Llama-3.1-8B-Instruct
  validation:
    provider: vllm
    model_id: meta-llama/Llama-3.1-8B-Instruct
  quiz_generation:
    provider: vllm
    model_id: meta-llama/Llama-3.1-8B-Instruct

cost:
  max_per_book: 0.50
